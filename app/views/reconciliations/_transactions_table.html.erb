<tbody id="reconciliation_transactions">
  <tbody id="reconciliation_transactions">
    <%= render partial: "reconciliations/transaction_row",
              collection: reconciliation.transactions.order(date: :asc),
              as: :tx %>
  </tbody>

</tbody>
