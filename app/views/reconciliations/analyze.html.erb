<h2>Transactions détectées dans le relevé</h2>

<% if @transactions.any? %>
  <table id="goals_table">
    <thead>
      <tr>
        <th>Date</th>
        <th>Description</th>
        <th>Montant</th>
      </tr>
    </thead>
    <tbody>
      <% @transactions.each do |t| %>
        <tr>
          <td><%= t[:date]&.strftime("%d/%m") %></td>
          <td><%= t[:description] %></td>
          <td style="color: <%= t[:amount] < 0 ? 'red' : 'green' %>;">
            <%= number_to_currency(t[:amount], unit: "€") %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>Aucune transaction détectée. 😕</p>
<% end %>

<%= link_to "Analyser un autre PDF", new_reconciliation_path, class: "btn btn-primary" %>
