<h2 class="balance-text">
  Transactions du relevé <strong><%= @reconciliation.name %></strong>
</h2>

<div id="goals_table" data-controller="match-toggle">
  <% if @transactions.any? %>
    <table>
      <thead>
        <tr>
          <th>Date</th>
          <th>Description</th>
          <th>Montant</th>
          <th>Match Score</th>
          <th>Type</th>
          <th>Actions</th>
          <th>Statut</th>
        </tr>
      </thead>
      <tbody>
        <%= render partial: "reconciliations/transaction_row", collection: @transactions, as: :tx %>
      </tbody>
    </table>
  <% else %>
    <p>Aucune transaction trouvée. 😕</p>
  <% end %>
</div>
