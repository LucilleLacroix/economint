<%= turbo_stream.replace "category_form" do %>
  <%= render "form", category: @category, category_type: @category.category_type %>
<% end %>

<%= turbo_stream.replace "categories_list" do %>
  <%= render "table", categories: current_user.categories.where(category_type: @category.category_type) %>
<% end %>
<%= turbo_stream.replace "flash",
      partial: "shared/flash" %>
